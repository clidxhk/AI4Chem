# AGENTS.md — AI4Chem 共学项目说明

本文件向所有协作者（包括 AI 编程助手）说明本项目的目的、结构与规范。开始任何工作前请先阅读本文。

## 一、项目是做什么的

本仓库是课题组 **AI for Chem 共学班** 的资料库与成果沉淀地。

共学班以"自主学习 + 集体讨论"为核心形式：

- 第一期持续 **3 个月，共 12 次定时讨论**，每次 30–40 分钟；
- 每周围绕一个主题（如"计算机是什么？"），成员课前各自准备一页 PPT/PDF（允许 AI 辅助生成）；
- 讨论时轮流分享、互相点评，最后 3 分钟由组织者总结，并确定下一周主题；
- 每次讨论的内容都会整理归档到本仓库，最终汇编成一份 **面向化学研究者的 AI for Chem 实用学习手册**。

## 二、目录结构

| 目录 | 内容 |
|---|---|
| `背景/` | 共学班的计划、招募说明等背景材料 |
| `第一次/`、`第二次/`… | 每次活动一个目录（中文序号命名），存放当次 PPT、录音转写记录、成员笔记等原始材料 |
| `手册/` | 最终交付物：由讨论记录整理成的讲义章节（编写规范见 `手册/AGENTS.md`） |

每次活动目录内的典型文件：

- `AI for CHEM.pptx` — 当次讨论用的 PPT；
- `记录.txt` — 录音转写的原始记录（保留口语原貌，作为一手材料）；
- `<成员姓名> - <主题>.docx/.pdf` — 各成员的课前笔记（保留原文件名与格式，不做改写）；
- `<主题>讨论笔记对比*.md` — 整理后的对比分析。

## 三、内容规范

1. **原始材料与整理成果分离**：转写记录、成员笔记等原始材料保持原样存入对应活动目录；加工整理只发生在 `手册/` 及对比分析类文档中。
2. **手册规范已迁移至子目录**：在 `手册/` 下新增或修改讲义前，必须先阅读 `手册/AGENTS.md`——双版本（课程版/通用版）、匿名化、章节命名、结构编号等详细要求均在该文件中。
3. **文件命名**：目录用中文序号（`第一次/`）；成员笔记保留作者原名以便溯源。
4. **写作语言**：正文使用中文；专业术语、工具名可保留英文原文。
5. **格式**：笔记与讲义用 Markdown；原始 PPT/DOCX/PDF 不做格式转换，原样保存。

## 四、Git 工作流（强制要求）

**每次修改本仓库后，必须执行完整的提交与推送流程**，不允许将改动长时间留在本地：

```bash
git add -A                      # 或具体文件路径
git commit -m "<中文提交信息>"    # 简明描述本次改了什么、为什么
git push origin master           # 推送到远程
```

具体要求：

1. **提交信息用中文**，简明说明本次变更内容，例如"新增第二次讨论五人笔记对比分析"、"手册第一部分通用版匿名化修订"；
2. **一次提交对应一个明确目的**，不要把无关改动混在同一个提交里；
3. **修改完成后立即提交并 push 到远程**（remote 为 `origin`，主分支为 `master`），确保 GitHub 上的仓库始终是最新状态；
4. 推送前如远程有新提交，先 `git pull --rebase` 再推送；
5. 本仓库为共学资料库，**禁止 force push、禁止改写已推送的历史**。

## 五、当前进度备忘

- 第一次讨论（相互认识与需求摸底）：材料已归档，手册第一部分已整理出课程版与通用版；
- 第二次讨论（计算机的本质）：五人笔记与对比分析已归档，手册第二部分（课程版 + 通用版）已整理；
- 第三次讨论（数据和信息）：议题说明与讲义五问已发布（`第三次/`），课前实践任务为安装 Anaconda＋VS Code；五份成员笔记与当次 PPT 已入库，对比分析待整理；
- 第四次讨论（机器学习入门＋阶段复盘）：议题说明与讲义五问已发布（`第四次/`；五问为层级、流派、范式、模型、评估与解释），课前实践任务为注册 GitHub、安装 Git 与 GitHub Desktop 并克隆共学班仓库（高阶选做：用 GitHub Pages 搭建个人学术主页）；
- 后续路线：第五次起启动 LogP 性质预测练手项目；
- 后续活动按同样的方式新增目录与归档材料。
